import styled from 'styled-components';

export const RatingsContainer = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;
    font-size: 0.8rem;
    flex: 1;
    gap: 0.5rem;
`;
